export const processPayment = async (cardNumber, expiry, cvv) => {
  // Add logic for payment processing, such as calling a payment gateway API
  console.log('Processing payment...');
  return { success: true }; // Mock success response
};
